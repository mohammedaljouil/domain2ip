# domain2ip
A fast, caching command-line tool that resolves domain names to their IPv4 and IPv6 addresses by querying a public DNS server directly. It bypasses your system’s default resolver and runs **A** and **AAAA** lookups in parallel for maximum speed.
